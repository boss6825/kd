# Base URL of the KD backend. The Better Auth client talks to
# `${NEXT_PUBLIC_API_BASE_URL}/api/auth`, and all API calls hit this host.
# Auth now lives entirely in the backend — the frontend needs no DB/auth keys.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
